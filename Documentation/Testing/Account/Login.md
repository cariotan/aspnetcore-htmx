## View

Sign in with no email or password: Required field message for both email and password.

Initial password eye is "hide password." Clicking toggles it.

Email input but no password: Password field error.

Password input but no email: Email field error.

Email input has validation for HTML email.

Both email and password input, but invalid: Invalid email and password error with no specificity on what the error is.

Clicking on "Register here" goes to /Register. Hover shows cursor: pointer.

It has "Continue with Google" and "Continue with GitHub."

**TODO:** Forgot password.

Check URL and make sure it's right and also that HTMX is used.